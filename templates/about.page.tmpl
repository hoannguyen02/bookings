{{define "content"}}
<h1>{{index .StringMap "desc"}}</h1>
<p>
    {{ if ne (index .StringMap "remote_ip") ""}}
        Your ip address is {{index .StringMap "remote_ip"}}
    {{else}}
        I don't know your ip address yet, Visit the <a href="/">Home page</a> so I can set it
    {{end}}
</p>
{{end}}